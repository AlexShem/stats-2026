---
title: "Семинар 2. Дискретные случайные величины: биномиальный закон и закон Пуассона"
subtitle: "Теория вероятностей и математическая статистика · НИЯУ МИФИ"
date: 2026-09-08
---

```{python}
#| label: setup
#| include: false
import numpy as np
import matplotlib.pyplot as plt
from math import comb, exp, lgamma, log
from fractions import Fraction as F

from mephi_stats import distribution_polygon, setup_matplotlib

setup_matplotlib()
rng = np.random.default_rng(2026)


def binom_pmf(n, p, k):
    """P(X = k) для X ~ Bi(n, p). Дроби сохраняются, если p — Fraction."""
    return comb(n, k) * p**k * (1 - p) ** (n - k)


def binom_row(n, p):
    return [binom_pmf(n, p, k) for k in range(n + 1)]


def pois_pmf(lam, k):
    """P(X = k) для X ~ Pois(λ).

    Считается через логарифмы: прямая формула λ^k·e^(-λ)/k! переполняется
    уже при λ = 50, k = 199 — числитель и знаменатель огромны по отдельности,
    хотя частное мало́.
    """
    return exp(k * log(lam) - lam - lgamma(k + 1))


def hyper_row(N, K, n):
    """Гипергеометрический ряд в точных дробях: K «удачных» из N, выбираем n."""
    return [F(comb(K, k) * comb(N - K, n - k), comb(N, n)) for k in range(n + 1)]


def show_row(values, probs, fmt="{:.4g}"):
    """Ряд распределения строкой — для печати под ячейкой."""
    head = "  ".join(f"{v:>8}" for v in values)
    body = "  ".join(f"{fmt.format(float(p)):>8}" for p in probs)
    return f"X: {head}\np: {body}\nΣp = {float(sum(probs)):.10f}"
```

::: {.content-visible when-profile="prep"}
::: {.teacher-note}
**Источник.** Гмурман, гл. 4 § 1 (с. 52–60) и § 2 (с. 60–63). Условия взяты из
`problems/gmurman/ch04-01.md` и `ch04-02.md`; ответы сверены со с. 374
(`answers-p374.md`), пересчитаны независимо. PDF не открывать.

**Привязка к лекции.** Лекция 3 (`lectures/03-discrete-random-variables.md`):
случайная величина, ряд распределения, многоугольник, биномиальный закон, закон
Пуассона. Держимся лекционных обозначений: $P_n(k)$, $\lambda = np$.

::: {.callout-important}
**Задачу 179 Гмурмана на семинаре не решаем** — лектор разобрал её дословно
(500 билетов, $p = 0{,}002$, $\lambda = 1$, те же четыре пункта). Она наша
опора для @sec-t5, где всё то же самое при $\lambda = 3$. Так и надо
подать: «формулу вы видели, посмотрим, что она умеет».
:::

**Чего в лекции не было, а в задачах есть.** Гипергеометрическое распределение
(@sec-t4) и геометрическое (@sec-t7, @sec-t9, @sec-t10). Оба вводим на
семинаре с нуля, от теорем сложения и умножения — то есть ровно от материала
семинара 01. Это главная содержательная связка занятия: **не всякая
дискретная величина биномиальна**.

**Состав.** Двенадцать задач, по четыре в блоке.

| Блок | Задачи (номера Гмурмана) | Что отрабатываем |
|:--|:--|:--|
| A. Разминка | 165, 167, 169, 171 | ряд, многоугольник, формула Бернулли, границы её применимости |
| B. Основной | 180, 177+178, 173, 183 | Пуассон в четырёх видах, $\lambda = np$, геометрический ряд, обратная задача |
| C. Для сильных | 174, 175, 182, 184 | сцепленные величины, чередование, нормировка ряда, простейший поток |

**План на 90 минут.**

| Время | Что |
|---:|:--|
| 10 мин | Сводка: ряд распределения, контроль $\sum p_i = 1$, Бернулли, Пуассон |
| 20 мин | Блок A — 165 и 167 быстро у доски, на 169 и 171 останавливаемся |
| 30 мин | Блок B — 180 к доске, 177/178 устно, 173 и 183 в парах |
| 25 мин | Блок C — 174 показываю сам, 175 следом, 182 и 184 сколько успеем |
| 5 мин | Домашнее задание, итоги |

Резерв: если группа идёт медленно, снимаем 184 (это уже § 2, следующая тема)
и 182 — оба восстанавливаются из конспекта. Если быстро — 184 стоит дать
обязательно, он готовит показательное распределение.

**Где студенты спотыкаются.**

- **Забывают контроль $\sum p_i = 1$.** Требовать его в каждой задаче
  с первой минуты: это единственная бесплатная проверка, которая у них есть.
- **Задача 169:** читают «чётное число очков на двух костях» как «чётная
  сумма» и получают $p = 1/2$ вместо $1/4$. Разбор — в @sec-t3.
- **Задача 171:** применяют формулу Бернулли к выборке без возвращения.
  А ещё выписывают значение $X = 0$, которого не существует.
- **Пуассон:** считают, что $\lambda$ — это $n$ или $p$, а не их произведение.
  Пара 177/178 (@sec-t6) сделана ровно против этого.
- **Задача 173:** ответ $k_0 = 1$ кажется противоречащим здравому смыслу
  («стрелок хороший!»). Здесь путают моду и среднее — см. @sec-t7.
- **Задача 174:** теряют, что $Y$ начинается с нуля, а $X$ — с единицы.

**Численная проверка.** Каждая задача сопровождается расчётом на Python:
точные дроби, полный перебор или симуляция. Если возникнет спор об ответе,
код под рукой.
:::
:::

## Краткая сводка

**Случайная величина** — величина, которая в результате опыта принимает одно из
возможных значений, заранее неизвестно какое. **Дискретной** называют такую,
чьи возможные значения можно перенумеровать (конечное или счётное множество).

**Закон распределения** дискретной величины $X$ — перечень возможных значений
$x_1, x_2, \dots$ и их вероятностей $p_i = \Prob(X = x_i)$. Задаётся таблицей
(**ряд распределения**):

| $X$ | $x_1$ | $x_2$ | … | $x_n$ |
|:--|--:|--:|:-:|--:|
| $p$ | $p_1$ | $p_2$ | … | $p_n$ |

::: {.callout-important}
**Контроль.** Всегда
$$
\sum_i p_i = 1,
$$ {#eq-norm}
потому что события $\{X = x_i\}$ несовместны и образуют полную группу. Для
бесконечного ряда — это сходящийся ряд с суммой 1. Проверяйте @eq-norm
в каждой задаче: почти любая арифметическая ошибка себя тут выдаёт.
:::

**Многоугольник распределения** — ломаная через точки $(x_i, p_i)$.

**Биномиальный закон.** $X$ — число появлений события в $n$ независимых
испытаниях с одинаковой вероятностью успеха $p$ ($q = 1 - p$):
$$
P_n(k) = \Prob(X = k) = C_n^k\, p^k q^{\,n-k}, \qquad k = 0, 1, \dots, n.
$$ {#eq-bernoulli}
Обозначение: $X \sim \mathrm{Bi}(n, p)$.

**Закон Пуассона.** Если $n$ велико, а $p$ мало, то при $\lambda = np$
$$
P_n(k) \approx \frac{\lambda^k e^{-\lambda}}{k!}, \qquad k = 0, 1, 2, \dots
$$ {#eq-poisson}
Параметр $\lambda$ — среднее число появлений события в $n$ испытаниях.

**Геометрический закон.** Испытания повторяются до первого «стоп-события»
с вероятностью $q$ в каждом; $X$ — номер испытания, на котором оно случилось:
$$
\Prob(X = k) = (1-q)^{\,k-1} q, \qquad k = 1, 2, \dots
$$ {#eq-geom}
Проверка @eq-norm — сумма геометрической прогрессии: $q/(1 - (1-q)) = 1$.

**Гипергеометрический закон.** Из $N$ объектов, среди которых $K$ «удачных»,
выбирают $n$ **без возвращения**; $X$ — число удачных среди выбранных:
$$
\Prob(X = k) = \frac{C_K^k\, C_{N-K}^{\,n-k}}{C_N^n}.
$$ {#eq-hyper}

::: {.callout-note}
**Как отличить одно от другого.** Формула Бернулли @eq-bernoulli требует, чтобы
испытания были **независимы** и вероятность успеха **не менялась**. Взяли
деталь и не вернули — вероятность изменилась, и работает @eq-hyper, а не
@eq-bernoulli. Ждём до первого промаха — число испытаний не фиксировано,
и работает @eq-geom. Первый вопрос к любой задаче этого занятия: **что здесь
за схема?**
:::

# Блок A. Разминка

## Задача 1 (Гмурман, гл. 4 § 1, № 165) {#sec-t1}

Дискретная случайная величина $X$ задана законом распределения:

а)

| $X$ | 2 | 4 | 5 | 6 |
|:--|--:|--:|--:|--:|
| $p$ | 0,3 | 0,1 | 0,2 | 0,4 |

б)

| $X$ | 10 | 15 | 20 |
|:--|--:|--:|--:|
| $p$ | 0,1 | 0,7 | 0,2 |

Построить многоугольник распределения.

::: {.content-visible when-profile="prep"}
::: {.solution}
Считать нечего: надо отложить точки $(x_i, p_i)$ и соединить их отрезками.
Единственное, что здесь проверяется, — это контроль @eq-norm:
$$
\text{а)}\; 0{,}3 + 0{,}1 + 0{,}2 + 0{,}4 = 1, \qquad
\text{б)}\; 0{,}1 + 0{,}7 + 0{,}2 = 1.
$$
Оба ряда корректны.

Важно проговорить, что́ многоугольник **не** означает: отрезки между вершинами
не имеют вероятностного смысла. Величина $X$ не принимает значения 3 или 4,5;
ломаная — способ увидеть форму распределения, а не интерполяция.

```{python}
#| label: fig-t1
#| fig-cap: "Многоугольники распределения для рядов а) и б). Отрезки соединяют вершины лишь для наглядности: промежуточные значения величина не принимает."
rows = {
    "а)": ([2, 4, 5, 6], [0.3, 0.1, 0.2, 0.4]),
    "б)": ([10, 15, 20], [0.1, 0.7, 0.2]),
}

fig, axes = plt.subplots(1, 2, figsize=(9.5, 3.6))
for ax, (name, (xs, ps)) in zip(axes, rows.items()):
    distribution_polygon(ax, xs, ps)
    ax.set_title(f"{name}  $\\sum p_i = {sum(ps):.0f}$")
    ax.set_xlabel("$x_i$")
plt.show()
```
:::

::: {.teacher-note}
Три минуты, один студент к доске на оба пункта. Единственный вопрос, который
стоит задать: «а чему равна вероятность того, что $X = 3$?» (пункт а). Ответ —
нулю, значения 3 у величины нет. Кто-то посмотрит на ломаную и назовёт
$0{,}2$ — это и есть та самая путаница, ради которой задача стоит первой.
:::
:::

## Задача 2 (Гмурман, гл. 4 § 1, № 167) {#sec-t2}

В партии 10% нестандартных деталей. Наудачу отобраны четыре детали. Написать
биномиальный закон распределения дискретной случайной величины $X$ — числа
нестандартных деталей среди четырёх отобранных и построить многоугольник
полученного распределения.

::: {.content-visible when-profile="prep"}
::: {.solution}
Схема Бернулли: $n = 4$ независимых отбора, $p = 0{,}1$, $q = 0{,}9$. По
@eq-bernoulli $P_4(k) = C_4^k (0{,}1)^k (0{,}9)^{4-k}$:
$$
P_4(0) = 0{,}9^4 = 0{,}6561, \qquad
P_4(1) = 4 \cdot 0{,}1 \cdot 0{,}9^3 = 0{,}2916,
$$
$$
P_4(2) = 6 \cdot 0{,}01 \cdot 0{,}81 = 0{,}0486, \qquad
P_4(3) = 4 \cdot 0{,}001 \cdot 0{,}9 = 0{,}0036, \qquad
P_4(4) = 0{,}0001.
$$

| $X$ | 0 | 1 | 2 | 3 | 4 |
|:--|--:|--:|--:|--:|--:|
| $p$ | 0,6561 | 0,2916 | 0,0486 | 0,0036 | 0,0001 |

Контроль @eq-norm выполнен.

**Тонкость, которую стоит проговорить.** Детали отбирают из партии, то есть
без возвращения, — строго говоря, это @eq-hyper, а не @eq-bernoulli. Формула
Бернулли применима потому, что партия большая: доля 10% практически не меняется
от изъятия четырёх деталей. В @sec-t4 партия будет из шести деталей, и там этот
номер уже не пройдёт.

```{python}
#| label: t2-check
row = binom_row(4, 0.1)
print(show_row(range(5), row))
```

```{python}
#| label: fig-t2
#| fig-cap: "Многоугольник биномиального распределения $\\mathrm{Bi}(4;\\,0{,}1)$. Распределение резко скошено вправо: вероятность встретить более двух нестандартных деталей из четырёх меньше 0,004."
fig, ax = plt.subplots(figsize=(7, 3.8))
distribution_polygon(ax, list(range(5)), row)
ax.set_xlabel("Число нестандартных деталей, $k$")
plt.show()
```
:::

::: {.teacher-note}
Первая задача, где применяется формула Бернулли, — стоит написать её на доске
в общем виде и подставлять при всех на глазах. Просить обязательно посчитать
$\sum p_i$: здесь она равна 1 «некрасиво» (0,6561 + 0,2916 + …), и это
хорошая тренировка аккуратности.

Замечание про «партия большая» **обязательно**, иначе @sec-t4 через двадцать
минут будет решаться по Бернулли.
:::
:::

## Задача 3 (Гмурман, гл. 4 § 1, № 169) {#sec-t3}

Две игральные кости одновременно бросают два раза. Написать биномиальный закон
распределения дискретной случайной величины $X$ — числа выпадений чётного числа
очков на двух игральных костях.

::: {.content-visible when-profile="prep"}
::: {.solution}
Задача проверяет не формулу Бернулли, а умение выделить испытание и посчитать
вероятность успеха.

**Испытание** — один бросок пары костей, значит $n = 2$ (бросают дважды).

**Успех** — «выпало чётное число очков на двух костях», то есть чётное число
очков на **каждой** из них:
$$
p = \tfrac12 \cdot \tfrac12 = \tfrac14, \qquad q = \tfrac34.
$$
Дальше @eq-bernoulli:
$$
P_2(0) = \left(\tfrac34\right)^2 = \tfrac{9}{16}, \qquad
P_2(1) = C_2^1 \cdot \tfrac14 \cdot \tfrac34 = \tfrac{6}{16}, \qquad
P_2(2) = \left(\tfrac14\right)^2 = \tfrac{1}{16}.
$$

| $X$ | 0 | 1 | 2 |
|:--|--:|--:|--:|
| $p$ | 9/16 | 6/16 | 1/16 |

Контроль: $9/16 + 6/16 + 1/16 = 1$.

**Ловушка.** Условие можно прочитать и как «выпала чётная **сумма** очков» —
тогда $p = 1/2$, и ответ был бы $1/4$, $1/2$, $1/4$. Ответ Гмурмана на с. 374
($9/16$, $6/16$, $1/16$) отвечает $p = 1/4$, то есть книга имеет в виду
«на каждой кости чётное». Это, кстати, единственное, чем задача 169 отличается
от задачи 168 (монета) — при чтении «чётная сумма» они дали бы один и тот же
ряд.

```{python}
#| label: t3-check
# Перебор всех 36 исходов броска пары костей — две трактовки условия
pairs = [(a, b) for a in range(1, 7) for b in range(1, 7)]
p_both = F(sum((a % 2 == 0) and (b % 2 == 0) for a, b in pairs), 36)
p_sum = F(sum((a + b) % 2 == 0 for a, b in pairs), 36)
print(f"«чётно на каждой кости»: p = {p_both}      «чётная сумма»: p = {p_sum}")

for name, p in (("p = 1/4 (наш ответ)", p_both), ("p = 1/2 (другое чтение)", p_sum)):
    print(f"\n{name}")
    print(show_row(range(3), binom_row(2, p), fmt="{:.4f}"))
print("\nкнига (с. 374): 9/16  6/16  1/16  ->  верно чтение p = 1/4")
```
:::

::: {.teacher-note}
**Дать группе прочитать условие и спросить, чему равно $p$.** Почти наверняка
прозвучат оба варианта — это лучший момент занятия, чтобы объяснить, что
формула Бернулли тривиальна, а вся работа сидит в определении испытания
и успеха.

Разрешать спор ссылкой на ответ книги, а не голосованием: показать
`answers-p374.md`. Заодно честный сюжет про то, что условия бывают
двусмысленными и надо уметь фиксировать своё прочтение явно.

Второй вопрос, если пошло бодро: сколько всего исходов у эксперимента?
($36^2 = 1296$, а величина $X$ принимает всего три значения — вот зачем
нужна случайная величина: она сворачивает громоздкое $\Omega$ до трёх чисел.)
:::
:::

## Задача 4 (Гмурман, гл. 4 § 1, № 171) {#sec-t4}

В партии из шести деталей имеется четыре стандартных. Наудачу отобраны три
детали. Составить закон распределения дискретной случайной величины $X$ — числа
стандартных деталей среди отобранных.

::: {.content-visible when-profile="prep"}
::: {.solution}
**Здесь формула Бернулли не работает.** Партия из шести деталей, берут три без
возвращения: вероятность вытащить стандартную меняется от извлечения
к извлечению. Схема — @eq-hyper с $N = 6$, $K = 4$, $n = 3$.

**Возможные значения.** Нестандартных всего две, значит среди трёх отобранных
стандартных не меньше одной: $X \in \{1, 2, 3\}$, и значения $0$ у величины
**нет**. Знаменатель $C_6^3 = 20$:
$$
\Prob(X = 1) = \frac{C_4^1 C_2^2}{20} = \frac{4}{20} = \frac15, \qquad
\Prob(X = 2) = \frac{C_4^2 C_2^1}{20} = \frac{12}{20} = \frac35,
$$
$$
\Prob(X = 3) = \frac{C_4^3 C_2^0}{20} = \frac{4}{20} = \frac15.
$$

| $X$ | 1 | 2 | 3 |
|:--|--:|--:|--:|
| $p$ | 1/5 | 3/5 | 1/5 |

Контроль: $1/5 + 3/5 + 1/5 = 1$.

```{python}
#| label: t4-check
row_h = hyper_row(6, 4, 3)
print("гипергеометрический ряд (точные дроби):")
for k, p in enumerate(row_h):
    print(f"  P(X = {k}) = {p}")
print(f"  сумма = {sum(row_h)}")

# Полный перебор: какие тройки можно вытащить из шести деталей
from itertools import combinations

parts = ["ст", "ст", "ст", "ст", "нест", "нест"]
draws = list(combinations(range(6), 3))
counts = {}
for d in draws:
    k = sum(parts[i] == "ст" for i in d)
    counts[k] = counts.get(k, 0) + 1
print(f"\nперебор всех C(6,3) = {len(draws)} троек: "
      + "  ".join(f"k={k}: {v}/{len(draws)}" for k, v in sorted(counts.items())))
```

**Сравнение с «наивным» биномиальным расчётом.** Если бы кто-то применил
@eq-bernoulli с $p = 4/6 = 2/3$, он получил бы заметно другое, и главное —
ненулевую вероятность значения $X = 0$, которого в этой задаче просто не
бывает.

```{python}
#| label: fig-t4
#| fig-cap: "Гипергеометрическое распределение (выбор без возвращения) против биномиального с той же долей успеха $p = 2/3$. Биномиальное приписывает вероятность значению $X = 0$, невозможному при шести деталях, и заметно шире."
row_b = binom_row(3, F(2, 3))

fig, ax = plt.subplots(figsize=(7, 4))
distribution_polygon(ax, list(range(4)), [float(p) for p in row_h],
                     label="без возвращения (гипергеом.)", color="#0b5394")
distribution_polygon(ax, list(range(4)), [float(p) for p in row_b],
                     label="с возвращением (Бернулли, $p=2/3$)", color="#b45309",
                     annotate=False)
ax.set_xlabel("Число стандартных деталей, $k$")
ax.legend(fontsize=9)
plt.show()
```
:::

::: {.teacher-note}
**Ключевая задача блока A.** Даю условие сразу после @sec-t2 и жду — почти
наверняка кто-то начнёт по Бернулли с $p = 2/3$. Не поправляю сразу, а прошу
досчитать до конца и выписать ряд. Там окажется $\Prob(X = 0) = 1/27 \ne 0$,
и вопрос «а как из шести деталей, где нестандартных всего две, вытащить три
нестандартные?» закрывает тему лучше любого объяснения.

Вторая ошибка, тише первой: правильно посчитали три вероятности, но выписали
таблицу с четырьмя столбцами, включая $X = 0$ с нулём. Формально не ошибка,
но стоит сказать, что перечень возможных значений — часть ответа.

Картинка выше — на экран, она показывает обе беды сразу.
:::
:::

# Блок B. Основной

## Задача 5 (Гмурман, гл. 4 § 1, № 180) {#sec-t5}

Магазин получил 1000 бутылок минеральной воды. Вероятность того, что при
перевозке бутылка окажется разбитой, равна $0{,}003$. Найти вероятности того,
что магазин получит разбитых бутылок: а) ровно две; б) менее двух; в) более
двух; г) хотя бы одну.

::: {.content-visible when-profile="prep"}
::: {.solution}
Ровно та задача, что разобрана на лекции (500 билетов, $\lambda = 1$), только
$\lambda$ другая. $n = 1000$ велико, $p = 0{,}003$ мало, события независимы —
применима @eq-poisson с
$$
\lambda = np = 1000 \cdot 0{,}003 = 3.
$$

а) $\Prob(X = 2) = \dfrac{3^2 e^{-3}}{2!} = \dfrac{9}{2}\,e^{-3} \approx 0{,}224$.

б) «Менее двух» — это $k = 0$ и $k = 1$:
$$
\Prob(X < 2) = e^{-3} + 3e^{-3} = 4e^{-3} \approx 0{,}1992.
$$

в) «Более двух» — через противоположное событие, иначе ряд бесконечен:
$$
\Prob(X > 2) = 1 - \big[\Prob(X<2) + \Prob(X=2)\big]
= 1 - (0{,}1992 + 0{,}2240) \approx 0{,}5768.
$$

г) «Хотя бы одна» — тоже через дополнение (приём семинара 01):
$$
\Prob(X \geqslant 1) = 1 - \Prob(X = 0) = 1 - e^{-3} \approx 0{,}950.
$$

**Четыре пункта — четыре стандартных типа вопроса,** и три из них решаются
переходом к противоположному событию. Это единственное, что надо унести из
задачи.

```{python}
#| label: t5-check
lam = 3.0
p = [pois_pmf(lam, k) for k in range(15)]
print(f"λ = np = 1000 · 0.003 = {lam:g}")
print("а) ровно две    P(X=2)  = %.5f   (книга 0,224)" % p[2])
print("б) менее двух   P(X<2)  = %.5f   (книга 0,1992)" % sum(p[:2]))
print("в) более двух   P(X>2)  = %.5f   (книга 0,5766)" % (1 - sum(p[:3])))
print("г) хотя бы одна P(X≥1)  = %.5f   (книга 0,95)" % (1 - p[0]))

# Точный биномиальный расчёт — насколько хорошо приближение
exact = [binom_pmf(1000, 0.003, k) for k in range(15)]
print("\nточно по Бернулли (n=1000, p=0.003):")
print("  а) %.5f   б) %.5f   в) %.5f   г) %.5f"
      % (exact[2], sum(exact[:2]), 1 - sum(exact[:3]), 1 - exact[0]))
print("  максимальное расхождение с Пуассоном: %.2e" % max(abs(a - b) for a, b in zip(p, exact)))
```
:::

::: {.teacher-note}
К доске, обязательно. Формулу студенты видели на лекции, поэтому задача идёт
быстро, и время тратится на правильное: три пункта из четырёх — переход
к противоположному событию, ровно как в задачах 47 и 82 семинара 01.
Стоит вслух связать: приём тот же, объект новый.

В пункте в) кто-нибудь предложит складывать $\Prob(X=3) + \Prob(X=4) + \dots$
— спросить, где остановиться. Ряд бесконечен, и это первый случай, когда
дополнение не «удобнее», а **единственно возможно**.

Расхождение Пуассона с точным биномиальным здесь порядка $10^{-4}$ (код выше);
если спросят «а насколько это приближение» — число под рукой.
:::
:::

## Задача 6 (Гмурман, гл. 4 § 1, № 177 и 178) {#sec-t6}

а) Устройство состоит из 1000 элементов, работающих независимо один от другого.
Вероятность отказа любого элемента в течение времени $T$ равна $0{,}002$. Найти
вероятность того, что за время $T$ откажут ровно три элемента.

б) Станок-автомат штампует детали. Вероятность того, что изготовленная деталь
окажется бракованной, равна $0{,}01$. Найти вероятность того, что среди 200
деталей окажется ровно четыре бракованных.

::: {.content-visible when-profile="prep"}
::: {.solution}
Обе — @eq-poisson, и обе имеют **одно и то же** $\lambda$:
$$
\text{а)}\; \lambda = 1000 \cdot 0{,}002 = 2, \qquad
\text{б)}\; \lambda = 200 \cdot 0{,}01 = 2.
$$

а) $P_{1000}(3) = \dfrac{2^3 e^{-2}}{3!} = \dfrac{8 \cdot 0{,}13534}{6}
\approx 0{,}180$.

б) $P_{200}(4) = \dfrac{2^4 e^{-2}}{4!} = \dfrac{16 \cdot 0{,}13534}{24}
\approx 0{,}090$.

**В этом вся соль.** У задач разные $n$ (1000 и 200) и разные $p$ (0,002
и 0,01), но одинаковое произведение. Пуассоновский ответ зависит **только**
от $\lambda = np$ — по отдельности $n$ и $p$ в формулу не входят. Значит,
это одно и то же распределение, и разными получились ответы лишь потому, что
спрашивали про разные $k$.

```{python}
#| label: t6-check
for name, n, p_, k in (("а)", 1000, 0.002, 3), ("б)", 200, 0.01, 4)):
    lam_ = n * p_
    print(f"{name} n = {n:5d}  p = {p_:<6g} λ = np = {lam_:g}   "
          f"Пуассон P({k}) = {pois_pmf(lam_, k):.5f}   "
          f"точно = {binom_pmf(n, p_, k):.5f}")
```

```{python}
#| label: fig-t6
#| fig-cap: "Одно $\\lambda = 2$, два разных биномиальных распределения. При $n = 200$ и $n = 1000$ точные ряды практически неразличимы и оба ложатся на пуассоновский предел — приближение работает тем лучше, чем больше $n$ при фиксированном $np$."
ks = list(range(9))
fig, ax = plt.subplots(figsize=(7.4, 4.2))
ax.bar([k - 0.16 for k in ks], [binom_pmf(200, 0.01, k) for k in ks],
       width=0.3, color="#b45309", alpha=0.85, label="$\\mathrm{Bi}(200;\\,0{,}01)$")
ax.bar([k + 0.16 for k in ks], [binom_pmf(1000, 0.002, k) for k in ks],
       width=0.3, color="#2e7d32", alpha=0.85, label="$\\mathrm{Bi}(1000;\\,0{,}002)$")
ax.plot(ks, [pois_pmf(2, k) for k in ks], "-o", color="#0b5394", lw=2, ms=6,
        label="$\\mathrm{Pois}(2)$")
ax.set_xlabel("$k$")
ax.set_ylabel("$\\mathsf{P}(X = k)$")
ax.set_xticks(ks)
ax.legend(fontsize=9)
plt.show()
```
:::

::: {.teacher-note}
Устно, обе за пять минут. **Не говорить заранее, что $\lambda$ совпадает** —
пусть посчитают и сами наткнутся. Вопрос после: «почему ответы разные, если
распределение одно?» (потому что спрашивают про разные $k$).

Это прямое лекарство от главной ошибки темы: студенты подставляют в формулу
Пуассона то $n$, то $p$. Если после этих двух задач кто-то ещё путается,
дальше уже не поможет ничего.

График на экран — на нём видно, что $n$ и $p$ по отдельности не важны.
:::
:::

## Задача 7 (Гмурман, гл. 4 § 1, № 173) {#sec-t7}

Вероятность того, что стрелок попадёт в мишень при одном выстреле, равна
$0{,}8$. Стрелку выдаются патроны до тех пор, пока он не промахнётся. Требуется:
а) составить закон распределения дискретной случайной величины $X$ — числа
патронов, выданных стрелку; б) найти наивероятнейшее число $k_0$ выданных
стрелку патронов.

::: {.content-visible when-profile="prep"}
::: {.solution}
**Новая схема:** число испытаний не задано заранее, стрельба идёт до первого
промаха. Формула Бернулли неприменима — в ней $n$ фиксировано.

а) Стрелку выдан ровно $k$-й патрон, если первые $k-1$ выстрелов были
попаданиями, а $k$-й — промахом. Выстрелы независимы, поэтому по теореме
умножения
$$
\Prob(X = k) = \underbrace{0{,}8 \cdots 0{,}8}_{k-1} \cdot 0{,}2
= 0{,}8^{\,k-1} \cdot 0{,}2, \qquad k = 1, 2, \dots
$$
Это @eq-geom с $q = 0{,}2$.

| $X$ | 1 | 2 | 3 | … | $k$ | … |
|:--|--:|--:|--:|:-:|--:|:-:|
| $p$ | 0,2 | 0,16 | 0,128 | … | $0{,}8^{k-1}\cdot 0{,}2$ | … |

**Контроль @eq-norm** — теперь это сумма бесконечной геометрической прогрессии:
$$
\sum_{k=1}^{\infty} 0{,}8^{\,k-1} \cdot 0{,}2
= \frac{0{,}2}{1 - 0{,}8} = 1. \;\checkmark
$$

б) Последовательность $0{,}8^{k-1}\cdot 0{,}2$ строго убывает по $k$, значит
наибольшая вероятность — у самого первого значения:
$$
k_0 = 1.
$$

**Ответ вызывает протест,** и правильно делает. Стрелок хороший, а вероятнее
всего ему выдадут один патрон? Да: $\Prob(X = 1) = 0{,}2$ больше любой другой
отдельной вероятности, хотя и мало само по себе. Величина $k_0$ — это **мода**
(самое вероятное значение), а не среднее. Среднее здесь
$\E X = 1/q = 1/0{,}2 = 5$, и вот оно уже отвечает интуиции. Мода и среднее —
разные вещи, и для скошенных распределений они далеко расходятся. Математическим
ожиданием займёмся на следующем занятии (§ 3).

```{python}
#| label: t7-check
q = 0.2
row_g = [(1 - q) ** (k - 1) * q for k in range(1, 41)]
print(show_row(range(1, 6), row_g[:5], fmt="{:.4f}"), "  (первые пять членов)")
print(f"\nсумма ряда (40 членов): {sum(row_g):.8f}   аналитически: {q / (1 - (1 - q)):.0f}")
print(f"мода k0 = {1 + int(np.argmax(row_g))}   среднее E[X] = 1/q = {1 / q:.0f}")

# симуляция: сколько патронов до первого промаха
trials = rng.random((200_000, 60)) < 0.8          # True = попадание
first_miss = np.argmin(trials, axis=1) + 1        # номер первого промаха
print(f"симуляция: мода = {np.bincount(first_miss).argmax()}, "
      f"среднее = {first_miss.mean():.3f}")
```

```{python}
#| label: fig-t7
#| fig-cap: "Геометрическое распределение числа выданных патронов, $q = 0{,}2$. Мода $k_0 = 1$ — в самом левом столбце, а среднее $\\mathsf{E}X = 5$ лежит далеко справа: у скошенных распределений «самое вероятное» и «в среднем» — разные числа."
ks = np.arange(1, 21)
fig, ax = plt.subplots(figsize=(7.4, 4))
ax.bar(ks, row_g[:20], color="#0b5394", alpha=0.85, width=0.65)
ax.axvline(1, color="#c62828", ls=":", lw=2)
ax.axvline(5, color="#2e7d32", ls="--", lw=2)
ax.text(1.25, 0.185, "мода $k_0 = 1$", color="#c62828", fontsize=11)
ax.text(5.25, 0.145, "среднее $\\mathsf{E}X = 5$", color="#2e7d32", fontsize=11)
ax.set_xlabel("Число выданных патронов, $k$")
ax.set_ylabel("$\\mathsf{P}(X = k)$")
ax.set_xticks(ks[::2])
plt.show()
```
:::

::: {.teacher-note}
В парах, потом разбор. Пункт а) идёт легко, а на б) начинается спор — его и надо
дать разгореться на минуту-полторы. Формулировка «наивероятнейшее число» звучит
как «сколько патронов он израсходует», и почти вся группа отвечает не 1.

Разрешение — на графике: мода слева, среднее справа. Это первое появление
математического ожидания на семинарах; определять его сейчас не нужно,
достаточно назвать и пообещать § 3.

Полезный контрольный вопрос: при каком $p$ мода перестанет быть единицей?
(Никогда: ряд убывает при любом $p < 1$. Для геометрического распределения
мода всегда 1 — а среднее растёт неограниченно.)
:::
:::

## Задача 8 (Гмурман, гл. 4 § 1, № 183) {#sec-t8}

Вероятность выигрыша по одному лотерейному билету $p = 0{,}01$. Сколько нужно
купить билетов, чтобы выиграть хотя бы по одному из них с вероятностью $P$,
не меньшей $0{,}95$?

::: {.content-visible when-profile="prep"}
::: {.solution}
Обратная задача: не «дано $n$, найти вероятность», а «дана вероятность, найти
$n$». Схема — «хотя бы один» через дополнение, $\Prob(A) = 1 - \Prob(\bar A)$,
ровно как в задачах 47 и 82 семинара 01, но с пуассоновским приближением.

Вероятность выигрыша мала, число билетов заведомо велико, поэтому число
выигрышных билетов приближённо пуассоново с $\lambda = np$. Полагая $k = 0$
в @eq-poisson, получаем $\Prob(X = 0) = e^{-\lambda}$, откуда
$$
P = 1 - \Prob(X = 0) = 1 - e^{-\lambda} \geqslant 0{,}95
\;\Longleftrightarrow\;
e^{-\lambda} \leqslant 0{,}05.
$$
Функция $e^{-x}$ убывает, поэтому неравенство выполняется при
$$
\lambda \geqslant -\ln 0{,}05 = 2{,}9957 \approx 3.
$$
По таблице Гмурман берёт ровно $e^{-3} = 0{,}05$, то есть $\lambda \geqslant 3$.
Наконец, $\lambda = np$ и $p = 0{,}01$:
$$
n \geqslant \frac{3}{0{,}01} = 300.
$$

**Надо купить не менее 300 билетов.**

*Проверка точным расчётом.* Без приближения условие выглядит как
$1 - 0{,}99^n \geqslant 0{,}95$, откуда $n \geqslant \ln 0{,}05/\ln 0{,}99
= 298{,}07$, то есть $n \geqslant 299$. Пуассоновское приближение ошиблось
на один билет из трёхсот — это и есть его точность.

```{python}
#| label: t8-check
# пуассоновское приближение
lam_min = -log(0.05)
print(f"Пуассон:  λ ≥ -ln 0.05 = {lam_min:.4f}  →  n ≥ λ/p = {lam_min / 0.01:.1f}  →  n = 300")

# точный биномиальный расчёт
n_exact = min(n for n in range(1, 2000) if 1 - 0.99**n >= 0.95)
print(f"Точно:    n ≥ ln 0.05 / ln 0.99 = {log(0.05) / log(0.99):.2f}  →  n = {n_exact}")
for n in (298, 299, 300):
    print(f"   n = {n}:  P(хотя бы один выигрыш) = {1 - 0.99**n:.5f}")
```
:::

::: {.teacher-note}
В парах. Главная трудность не в вычислении, а в переводе условия: «выиграть
хотя бы по одному» → «$1 - \Prob(X=0)$» → «$1 - e^{-\lambda}$». Кто на
семинаре 01 усвоил приём дополнения, справится за три минуты.

Смена знака при логарифмировании убывающей функции — то же место, где ломались
на задаче 60 в прошлый раз. Стоит напомнить явно.

Расхождение 299 против 300 — хороший финал: приближение не бесплатно. Если
спросят, какой ответ «правильный», — правильный 299, а книжный 300 отвечает
на вопрос в рамках пуассоновской модели и заведомо безопасен (билетов
куплено с запасом).
:::
:::

# Блок C. Для сильных

## Задача 9 (Гмурман, гл. 4 § 1, № 174) {#sec-t9}

Из двух орудий поочерёдно ведётся стрельба по цели до попадания одним из орудий.
Вероятность попадания в цель первым орудием равна $0{,}3$, вторым — $0{,}7$.
Начинает стрельбу первое орудие. Составить законы распределения дискретных
случайных величин $X$ и $Y$ — числа израсходованных снарядов соответственно
первым и вторым орудием.

::: {.content-visible when-profile="prep"}
::: {.solution}
Самая содержательная задача занятия: **два разных ряда из одного эксперимента**.

Обозначим $A_i$ и $B_i$ — попадания первого и второго орудия при $i$-м
выстреле; $p_1 = 0{,}3$, $q_1 = 0{,}7$, $p_2 = 0{,}7$, $q_2 = 0{,}3$.

**Ключевое наблюдение.** Стрельба идёт «раундами»: выстрел первого орудия,
затем, если промах, выстрел второго. Раунд завершает стрельбу, если попало
хоть одно орудие; продолжение возможно лишь при двух промахах, вероятность чего
$$
r = q_1 q_2 = 0{,}7 \cdot 0{,}3 = 0{,}21.
$$
Значит **обе** величины геометрические со знаменателем $0{,}21$, и различаются
только первым членом. Дальше — арифметика.

**Величина $X$** (снаряды первого орудия). Первое орудие израсходует один
снаряд, если оно попало сразу, либо промахнулось, но попало второе:
$$
\Prob(X = 1) = p_1 + q_1 p_2 = 0{,}3 + 0{,}7 \cdot 0{,}7 = 0{,}79.
$$
Чтобы первое орудие израсходовало $k$ снарядов, нужно, чтобы прошло $k-1$ полных
безрезультатных раундов, а затем сработал тот же сценарий:
$$
\Prob(X = k) = 0{,}79 \cdot 0{,}21^{\,k-1}, \qquad k = 1, 2, \dots
$$

**Величина $Y$** (снаряды второго орудия). Она начинается с **нуля**: если
первое орудие попало первым же выстрелом, второе не стреляло вовсе:
$$
\Prob(Y = 0) = p_1 = 0{,}3.
$$
Второе орудие израсходует один снаряд, если оно выстрелило и попало, либо
выстрелило, промахнулось, а первое попало в следующем раунде:
$$
\Prob(Y = 1) = q_1 p_2 + q_1 q_2 p_1 = 0{,}49 + 0{,}21 \cdot 0{,}3 = 0{,}553,
$$
и далее $\Prob(Y = k) = 0{,}553 \cdot 0{,}21^{\,k-1}$, $k \geqslant 1$.

**Контроль @eq-norm** для обеих:
$$
\sum_{k \geqslant 1} 0{,}79 \cdot 0{,}21^{k-1} = \frac{0{,}79}{1 - 0{,}21} = 1,
\qquad
0{,}3 + \sum_{k \geqslant 1} 0{,}553 \cdot 0{,}21^{k-1}
= 0{,}3 + \frac{0{,}553}{0{,}79} = 0{,}3 + 0{,}7 = 1.
$$

```{python}
#| label: t9-check
p1, q1, p2, q2 = 0.3, 0.7, 0.7, 0.3
r = q1 * q2
X1, Y0, Y1 = p1 + q1 * p2, p1, q1 * p2 + r * p1
print(f"знаменатель прогрессии r = q1·q2 = {r:.2f}")
print(f"P(X=1) = {X1:.3f}    P(Y=0) = {Y0:.3f}    P(Y=1) = {Y1:.3f}")
print(f"контроль ΣP(X) = {X1 / (1 - r):.6f}    ΣP(Y) = {Y0 + Y1 / (1 - r):.6f}")

# Симуляция дуэли
def duel(n, rng):
    x = np.zeros(n, dtype=int); y = np.zeros(n, dtype=int)
    alive = np.ones(n, dtype=bool)
    for _ in range(80):
        if not alive.any():
            break
        x[alive] += 1                                     # стреляет первое орудие
        hit1 = alive & (rng.random(n) < p1)
        alive &= ~hit1
        y[alive] += 1                                     # стреляет второе
        hit2 = alive & (rng.random(n) < p2)
        alive &= ~hit2
    return x, y

xs, ys = duel(200_000, rng)
print("\n           k=0      k=1      k=2      k=3")
print("X сим.        —   " + "  ".join(f"{(xs == k).mean():.4f}" for k in (1, 2, 3)))
print("X теор.       —   " + "  ".join(f"{X1 * r**(k - 1):.4f}" for k in (1, 2, 3)))
print(f"Y сим.   {(ys == 0).mean():.4f}   " + "  ".join(f"{(ys == k).mean():.4f}" for k in (1, 2, 3)))
print(f"Y теор.  {Y0:.4f}   " + "  ".join(f"{Y1 * r**(k - 1):.4f}" for k in (1, 2, 3)))
```

```{python}
#| label: fig-t9
#| fig-cap: "Ряды распределения $X$ и $Y$. Знаменатель прогрессии у обеих один — $0{,}21$, — но $Y$ начинается с нуля (первое орудие могло закрыть вопрос первым же выстрелом), а $X$ — с единицы: первое орудие стреляет всегда."
ks = np.arange(0, 6)
px = np.array([0.0] + [X1 * r ** (k - 1) for k in range(1, 6)])
py = np.array([Y0] + [Y1 * r ** (k - 1) for k in range(1, 6)])

fig, ax = plt.subplots(figsize=(7.4, 4.2))
ax.bar(ks - 0.17, px, width=0.32, color="#0b5394", alpha=0.9, label="$X$ — первое орудие")
ax.bar(ks + 0.17, py, width=0.32, color="#b45309", alpha=0.9, label="$Y$ — второе орудие")
ax.set_xlabel("Число израсходованных снарядов, $k$")
ax.set_ylabel("$\\mathsf{P}$")
ax.set_xticks(ks)
ax.legend(fontsize=10)
plt.show()
```
:::

::: {.teacher-note}
**Показываю сам**, к доске не вызываю: выкладки длинные, а суть в двух местах.

Первое — увидеть «раунд» и знаменатель $r = q_1 q_2 = 0{,}21$. Если это
произнесено, обе величины выписываются за минуту; если нет — начинается
бесконечное перечисление сценариев, и задача съедает весь блок.

Второе — почему $Y$ начинается с нуля, а $X$ с единицы. Спрашиваю прямо
и жду ответа от группы: первое орудие стреляет всегда, второе может вообще
не выстрелить.

Симуляция дуэли (код выше) — на случай спора про 0,553. Числа сходятся
в третьем знаке.
:::
:::

## Задача 10 (Гмурман, гл. 4 § 1, № 175) {#sec-t10}

Два бомбардировщика поочерёдно сбрасывают бомбы на цель до первого попадания.
Вероятность попадания в цель первым бомбардировщиком равна $0{,}7$, вторым —
$0{,}8$. Вначале сбрасывает бомбы первый бомбардировщик. Составить первые четыре
члена закона распределения дискретной случайной величины $X$ — числа сброшенных
бомб обоими бомбардировщиками (т. е. ограничиться возможными значениями $X$,
равными 1, 2, 3 и 4).

::: {.content-visible when-profile="prep"}
::: {.solution}
Тот же сюжет, что @sec-t9, но вопрос **противоположный**: там считали снаряды
каждого орудия по отдельности (две величины), здесь — общее число бомб (одна
величина). $X$ — это просто номер броска, на котором случилось первое попадание.

Броски чередуются: нечётные — первый бомбардировщик ($0{,}7$), чётные — второй
($0{,}8$). Значит $X = k$ означает «первые $k-1$ бросков промахи, $k$-й —
попадание»:
$$
\Prob(X = 1) = 0{,}7;
$$
$$
\Prob(X = 2) = 0{,}3 \cdot 0{,}8 = 0{,}24;
$$
$$
\Prob(X = 3) = 0{,}3 \cdot 0{,}2 \cdot 0{,}7 = 0{,}042;
$$
$$
\Prob(X = 4) = 0{,}3 \cdot 0{,}2 \cdot 0{,}3 \cdot 0{,}8 = 0{,}0144.
$$

| $X$ | 1 | 2 | 3 | 4 |
|:--|--:|--:|--:|--:|
| $p$ | 0,7 | 0,24 | 0,042 | 0,0144 |

**Почему книга просит только четыре члена.** Ряд не геометрический: вероятности
не образуют одной прогрессии, потому что вероятность попадания меняется от
броска к броску. Прогрессия появляется, если смотреть **через один**, парами:
знаменатель $0{,}3 \cdot 0{,}2 = 0{,}06$, и
$$
\Prob(X = 2m-1) = 0{,}06^{\,m-1} \cdot 0{,}7, \qquad
\Prob(X = 2m) = 0{,}06^{\,m-1} \cdot 0{,}24.
$$
Контроль @eq-norm тогда считается сразу по обеим подпоследовательностям:
$$
\sum_{m \geqslant 1} 0{,}06^{m-1}(0{,}7 + 0{,}24) = \frac{0{,}94}{1 - 0{,}06} = 1.
$$

```{python}
#| label: t10-check
a, b = 0.7, 0.8
row = [a, (1 - a) * b, (1 - a) * (1 - b) * a, (1 - a) * (1 - b) * (1 - a) * b]
print(show_row(range(1, 5), row, fmt="{:.4f}"), " (первые четыре члена, сумма < 1 — ряд продолжается)")
print(f"\nзнаменатель по парам: {(1 - a) * (1 - b):.2f}")
print(f"сумма полного ряда: {(a + (1 - a) * b) / (1 - (1 - a) * (1 - b)):.6f}")

# симуляция
n = 300_000
p_by_step = np.array([a if s % 2 == 0 else b for s in range(30)])
hits = rng.random((n, 30)) < p_by_step
first = np.argmax(hits, axis=1) + 1
print("\nсимуляция:", "  ".join(f"P(X={k}) = {(first == k).mean():.4f}" for k in range(1, 5)))
```
:::

::: {.teacher-note}
Сразу после @sec-t9, пока «раунд» ещё на доске. Первые четыре члена группа
выписывает сама за пять минут — они простые.

Настоящий вопрос задать после: **почему книга ограничилась четырьмя членами?**
Ответ — ряд не геометрический, прогрессия видна только по парам. Это отличает
задачу от 173 и 174 и стоит того, чтобы дописать общий член на доске, если
время есть.

Если времени нет — четырёх членов достаточно, задача засчитана.
:::
:::

## Задача 11 (Гмурман, гл. 4 § 1, № 182) {#sec-t11}

Доказать, что сумма вероятностей числа появлений события в независимых
испытаниях, вычисленных по закону Пуассона, равна единице.

::: {.content-visible when-profile="prep"}
::: {.solution}
Первая на занятии задача без чисел. Требуется проверить @eq-norm для
@eq-poisson, то есть что закон Пуассона вообще является законом распределения.

Множитель $e^{-\lambda}$ от $k$ не зависит и выносится за знак суммы:
$$
\sum_{k=0}^{\infty} P_n(k)
= \sum_{k=0}^{\infty} \frac{\lambda^k e^{-\lambda}}{k!}
= e^{-\lambda} \sum_{k=0}^{\infty} \frac{\lambda^k}{k!}.
$$
Оставшаяся сумма — ряд Маклорена для экспоненты,
$$
e^{x} = \sum_{k=0}^{\infty} \frac{x^k}{k!},
$$
сходящийся при любом $x$; при $x = \lambda$ он равен $e^{\lambda}$. Значит,
$$
\sum_{k=0}^{\infty} P_n(k) = e^{-\lambda} \cdot e^{\lambda} = e^{0} = 1.
\qquad \blacksquare
$$

**Замечание Гмурмана.** Утверждение следует немедленно из того, что сумма
вероятностей событий полной группы равна единице; доказательство приведено
в учебных целях.

Замечание стоит обсудить, а не проглотить. Оно верно для биномиального
закона — там события «ровно $k$ успехов», $k = 0, \dots, n$, действительно
образуют полную группу, и @eq-norm выполняется по построению. Но
пуассоновские вероятности @eq-poisson — это **приближение** биномиальных,
и то, что приближённые числа тоже дают в сумме ровно 1, требует проверки.
Выкладка выше её и делает.

```{python}
#| label: t11-check
for lam_ in (0.5, 1, 3, 10, 50):
    s = sum(pois_pmf(lam_, k) for k in range(200))
    print(f"λ = {lam_:>4}:  Σ P(X=k), k ≤ 199  =  {s:.15f}")
```
:::

::: {.teacher-note}
Короткая, десять минут вместе с обсуждением. Собственно выкладка — три строки,
и её достаточно продиктовать.

Вся ценность в замечании книги. Задать вопрос: «зачем доказывать то, что
и так очевидно из полной группы?» Ответ — потому что пуассоновские вероятности
не вероятности реальных событий, а приближение; их нормировка не гарантирована
даром. Это первый на семинарах случай, когда надо отличать модель от того, что
она приближает, и дальше по курсу это будет постоянно.

Если группа сильная, спросить, где именно используется, что ряд сходится
при любом $\lambda$ (в перестановке суммирования и вынесении множителя).
:::
:::

## Задача 12 (Гмурман, гл. 4 § 2, № 184) {#sec-t12}

Показать, что формулу Пуассона, определяющую вероятность появления $k$ событий
за время длительностью $t$,
$$
P_t(k) = \frac{(\lambda t)^k e^{-\lambda t}}{k!},
$$ {#eq-flow}
можно рассматривать как математическую модель простейшего потока событий; другими
словами, показать, что формула Пуассона отражает все свойства простейшего потока.

**Напоминание.** *Потоком событий* называют последовательность событий,
наступающих в случайные моменты времени. *Простейшим (пуассоновским)* называют
поток, обладающий тремя свойствами: **стационарность** (вероятность появления
$k$ событий зависит только от $k$ и длительности $t$ промежутка, но не от его
начала), **отсутствие последействия** (не зависит от того, что было до начала
промежутка), **ординарность** (появление двух и более событий за малое время
пренебрежимо маловероятно по сравнению с появлением одного).
*Интенсивностью* $\lambda$ называют среднее число событий в единицу времени.

::: {.content-visible when-profile="prep"}
::: {.solution}
Проверяем три свойства по очереди.

**Стационарность.** В правой части @eq-flow стоят только $k$, $t$ и постоянная
$\lambda$. Момент начала промежутка в формулу не входит вовсе — значит,
вероятность зависит лишь от $k$ и длительности $t$. ✓

**Отсутствие последействия.** Формула @eq-flow не содержит никакой информации
о том, что происходило до начала промежутка: нет ни числа предшествующих
событий, ни моментов их наступления. ✓

**Ординарность.** Положим $k = 0$ и $k = 1$:
$$
P_t(0) = e^{-\lambda t}, \qquad P_t(1) = \lambda t\, e^{-\lambda t}.
$$
Тогда вероятность появления более одного события
$$
P_t(k > 1) = 1 - \big[P_t(0) + P_t(1)\big]
= 1 - (1 + \lambda t)\,e^{-\lambda t}.
$$
Разложим $e^{-\lambda t}$ в ряд Маклорена. После приведения подобных
$$
P_t(k > 1) = \frac{(\lambda t)^2}{2} - \frac{(\lambda t)^3}{3} + \dots
$$
Сравним с $P_t(1) \approx \lambda t$ при малых $t$: отношение
$$
\frac{P_t(k>1)}{P_t(1)} \approx \frac{\lambda t}{2} \xrightarrow[t \to 0]{} 0.
$$
Значит, при малых $t$ вероятность появления более одного события пренебрежимо
мала по сравнению с вероятностью одного. ✓

Все три свойства выполнены, поэтому @eq-flow — математическая модель
простейшего потока. $\blacksquare$

**Что здесь по-настоящему нового.** Параметр $\lambda$ сменил смысл. В @eq-poisson
это было $\lambda = np$ — среднее число за $n$ испытаний; в @eq-flow это
интенсивность, среднее число **в единицу времени**, а роль числа испытаний $n$
играет время $t$. Формула та же, читается иначе.

```{python}
#| label: t12-check
import sympy as sp

t_s, lam_s = sp.symbols("t lambda", positive=True)
more_than_one = 1 - (1 + lam_s * t_s) * sp.exp(-lam_s * t_s)
print("P_t(k>1) =", sp.series(more_than_one, t_s, 0, 5))
print("\nординарность:  P(k>1)/P(1) →",
      sp.limit(more_than_one / (lam_s * t_s * sp.exp(-lam_s * t_s)), t_s, 0), " при t → 0")
```

```{python}
#| label: fig-t12
#| fig-cap: "Ординарность простейшего потока: при малых $t$ вероятность одного события растёт как $\\lambda t$, а вероятность двух и более — как $(\\lambda t)^2/2$, то есть на порядок медленнее. Оси логарифмические."
tt = np.logspace(-3, 0, 200)
lam_v = 1.0
p1_ = lam_v * tt * np.exp(-lam_v * tt)
pmore = 1 - (1 + lam_v * tt) * np.exp(-lam_v * tt)

fig, ax = plt.subplots(figsize=(7.2, 4.2))
ax.loglog(tt, p1_, lw=2, color="#0b5394", label="$P_t(1) \\approx \\lambda t$")
ax.loglog(tt, pmore, lw=2, color="#c62828", label="$P_t(k>1) \\approx (\\lambda t)^2/2$")
ax.loglog(tt, (lam_v * tt) ** 2 / 2, ls="--", lw=1.2, color="gray", label="$(\\lambda t)^2/2$")
ax.set_xlabel("Длительность промежутка $t$  (при $\\lambda = 1$)")
ax.set_ylabel("Вероятность")
ax.legend(fontsize=9, loc="lower right")
plt.show()
```
:::

::: {.teacher-note}
**Задача-мост, и её стоит дать, даже если время поджимает** — хотя бы устно,
без выкладки про ординарность. Она переводит тему из «$n$ испытаний» в «поток
во времени», а оттуда следующая лекция уйдёт в показательное распределение.

Первые два свойства проверяются взглядом на формулу — тридцать секунд.
Всё время уходит на ординарность; если его нет, достаточно сказать, что
$P_t(k>1)$ имеет порядок $t^2$, а $P_t(1)$ — порядок $t$.

Главное, что надо произнести вслух: **$\lambda$ поменяла смысл.** Студенты,
привыкшие за час к $\lambda = np$, механически ищут здесь $n$ и $p$ и не
находят. Написать на доске соответствие $n \leftrightarrow t$,
$np \leftrightarrow \lambda t$.

Если блок C идёт с опозданием, снимается первым.
:::
:::

# Задачи для самостоятельной работы

1. **(Гмурман, № 166.)** Устройство состоит из трёх независимо работающих
   элементов. Вероятность отказа каждого элемента в одном опыте равна $0{,}1$.
   Составить закон распределения числа отказавших элементов в одном опыте.
2. **(Гмурман, № 170.)** В партии из 10 деталей имеется 8 стандартных. Наудачу
   отобраны две детали. Составить закон распределения числа стандартных деталей
   среди отобранных. Сравните схему с задачей @sec-t4.
3. **(Гмурман, № 172.)** После ответа студента на вопросы экзаменационного
   билета экзаменатор задаёт дополнительные вопросы и прекращает, как только
   студент обнаруживает незнание заданного вопроса. Вероятность того, что
   студент ответит на любой заданный дополнительный вопрос, равна $0{,}9$.
   Требуется: а) составить закон распределения числа заданных дополнительных
   вопросов; б) найти наивероятнейшее число $k_0$ заданных вопросов.
4. **(Гмурман, № 181 б.)** Найти среднее число $\lambda$ бракованных изделий
   в партии, если вероятность того, что в партии содержится хотя бы одно
   бракованное изделие, равна $0{,}95$. Число бракованных изделий распределено
   по закону Пуассона. *(Указание: принять $e^{-3} = 0{,}05$.)*

::: {.content-visible when-profile="prep"}
::: {.teacher-note}
Ответы: 1) $\mathrm{Bi}(3;\,0{,}1)$ — 0,729; 0,243; 0,027; 0,001;
2) гипергеометрическое — $1/45$; $16/45$; $28/45$ (пара к @sec-t4, но здесь
$X = 0$ уже возможно: нестандартных две и отбирают две);
3) $\Prob(X = k) = 0{,}9^{k-1}\cdot 0{,}1$, $k_0 = 1$ (пара к @sec-t7);
4) $1 - e^{-\lambda} = 0{,}95 \Rightarrow \lambda = 3$ (пара к @sec-t8, но
короче: искать $n$ не надо).

Подбор намеренный: к каждой разобранной схеме — по одному близнецу.
Разборы всех четырёх — в `problems/gmurman/ch04-01.md`.

Задача 2 содержит маленькую ловушку, стоит её ожидать в разборе: в @sec-t4
значения $X = 0$ не было, а здесь оно есть, и студент, запомнивший «нуля не
бывает», потеряет столбец.
:::
:::
